// Import Modules
const express = require('express'); //server
const router = express.Router(); //router part of server

// Import Controller
const treeController = require('../controllers/diplomaController');
const userController = require('../controllers/studentsController');

module.exports = router;